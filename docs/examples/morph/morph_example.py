"""Removed morph example kept only as a tombstone during shared cleanup."""

from __future__ import annotations


def build():
    raise NotImplementedError(
        "The morph example has been removed because the morph capability is no longer supported."
    )
